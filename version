#!/bin/bash

dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

if [[ -f ${dir}/source/VERSION ]] ; then

    echo $(head -n 1 ${dir}/source/VERSION)

else

    >&2 echo "Cannot find version file!"
    exit 1

fi

exit 0
